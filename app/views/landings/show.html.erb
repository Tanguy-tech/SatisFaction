<p> Voici l'id de la landing : <%= @landing.id %> </p>
<p> Voici le survey id associé à la landing : <%= @landing.survey_id %> </p>


This is Survey N° <% @survey.each do |s| %>
                  <%= s.id %><br>
                <% end %><br>


Questions for this survey : <br><% @questions.each do |q| %>
                              <%= q.content %><br>
                            <% end %><br>

Answers for all Questions : <br><% @answers.each do |a| %><br>
                                  Rate :<%= a.value %><br>
                                  answer id : <%= a.id %><br>
                                  question_id : <%= a.question_id %><br>
                                  survey_id : <%= a.survey_id %><br>
                                  Created at : <%= a.created_at.strftime("%d %b %Y %k:%M") %>
                                <% end %><br>
<br>
  <% @questions.each do |q| %>
    <%= simple_form_for(@answer) do |f| %>
      <%= q.content %>
        <%= f.select :value, options_for_select(1..4) %>
        <%= f.hidden_field :question_id, :value => q.id %>
        <%= f.hidden_field :survey_id, :value => q.survey_id %>

        <%= f.button :submit, 'Rate', class: "btn btn-info btn-sm" %>
    <% end %>
  <% end %>